sudo apt install ruby-full
sudo apt install chruby
sudo apt install zoom
sudo apt install zsh #need to check this one
sudo apt install docker
sudo apt install asciinema
sudo apt install build-essential
#I want this to be formatted much better....may be worth going through and
#creating something more like a library to perform this stuff...
#need to go through and add in checks here
